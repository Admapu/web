{
  "name": "admapu-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:blog-routes": "node scripts/generate-blog-routes.mjs",
    "dev": "npm run prepare:blog-routes && vite",
    "build": "npm run prepare:blog-routes && vite build",
    "preview": "vite preview --host",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "wrangler": "^4.64.0"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.2.0",
    "marked": "^15.0.7"
  }
}
